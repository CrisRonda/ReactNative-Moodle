import React from 'react';

import {Container, Text} from 'native-base';
const Home = (params) => {
  return (
    <Container>
      <Text>home screen</Text>
    </Container>
  );
};

export default Home;
